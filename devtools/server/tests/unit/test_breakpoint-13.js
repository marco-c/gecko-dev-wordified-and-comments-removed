"
use
strict
"
;
add_task
(
threadClientTest
(
(
{
threadClient
debuggee
}
)
=
>
{
return
new
Promise
(
resolve
=
>
{
threadClient
.
addOneTimeListener
(
"
paused
"
async
function
(
event
packet
)
{
const
source
=
await
getSourceById
(
threadClient
packet
.
frame
.
where
.
actor
)
;
const
location
=
{
line
:
debuggee
.
line0
+
2
}
;
source
.
setBreakpoint
(
location
)
.
then
(
async
function
(
[
response
bpClient
]
)
{
const
testCallbacks
=
[
function
(
packet
)
{
Assert
.
equal
(
packet
.
frame
.
where
.
line
debuggee
.
line0
+
5
)
;
Assert
.
equal
(
packet
.
why
.
type
"
resumeLimit
"
)
;
}
function
(
packet
)
{
Assert
.
equal
(
packet
.
frame
.
where
.
line
location
.
line
)
;
Assert
.
notEqual
(
packet
.
why
.
type
"
breakpoint
"
)
;
Assert
.
equal
(
packet
.
why
.
type
"
resumeLimit
"
)
;
}
function
(
packet
)
{
Assert
.
equal
(
packet
.
frame
.
where
.
line
debuggee
.
line0
+
3
)
;
Assert
.
notEqual
(
packet
.
why
.
type
"
breakpoint
"
)
;
Assert
.
equal
(
packet
.
why
.
type
"
resumeLimit
"
)
;
}
function
(
packet
)
{
Assert
.
equal
(
packet
.
frame
.
where
.
line
debuggee
.
line0
+
3
)
;
Assert
.
notEqual
(
packet
.
why
.
type
"
breakpoint
"
)
;
Assert
.
equal
(
packet
.
why
.
type
"
resumeLimit
"
)
;
Assert
.
equal
(
packet
.
why
.
frameFinished
.
return
.
type
"
undefined
"
)
;
}
function
(
packet
)
{
Assert
.
equal
(
debuggee
.
a
1
)
;
Assert
.
equal
(
debuggee
.
b
undefined
)
;
Assert
.
equal
(
packet
.
frame
.
where
.
line
debuggee
.
line0
+
6
)
;
Assert
.
notEqual
(
packet
.
why
.
type
"
debuggerStatement
"
)
;
Assert
.
equal
(
packet
.
why
.
type
"
resumeLimit
"
)
;
Assert
.
equal
(
packet
.
poppedFrames
.
length
1
)
;
}
function
(
packet
)
{
Assert
.
equal
(
packet
.
frame
.
where
.
line
debuggee
.
line0
+
7
)
;
Assert
.
notEqual
(
packet
.
why
.
type
"
debuggerStatement
"
)
;
Assert
.
equal
(
packet
.
why
.
type
"
resumeLimit
"
)
;
}
]
;
for
(
const
callback
of
testCallbacks
)
{
const
waiter
=
waitForPause
(
threadClient
)
;
threadClient
.
stepIn
(
)
;
const
packet
=
await
waiter
;
callback
(
packet
)
;
}
const
waiter
=
waitForPause
(
threadClient
)
;
threadClient
.
stepIn
(
)
;
await
waiter
;
bpClient
.
remove
(
(
)
=
>
threadClient
.
resume
(
resolve
)
)
;
}
)
;
}
)
;
Cu
.
evalInSandbox
(
"
var
line0
=
Error
(
)
.
lineNumber
;
\
n
"
+
"
function
foo
(
)
{
\
n
"
+
"
this
.
a
=
1
;
\
n
"
+
"
}
\
n
"
+
"
debugger
;
\
n
"
+
"
foo
(
)
;
\
n
"
+
"
debugger
;
\
n
"
+
"
var
b
=
2
;
\
n
"
debuggee
)
;
}
)
;
}
)
)
;
