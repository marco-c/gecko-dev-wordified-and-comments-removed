use
crate
:
:
api
:
:
{
self
StorageChanges
}
;
use
crate
:
:
db
:
:
{
StorageDb
ThreadSafeStorageDb
}
;
use
crate
:
:
error
:
:
*
;
use
crate
:
:
migration
:
:
{
migrate
MigrationInfo
}
;
use
crate
:
:
sync
;
use
std
:
:
path
:
:
Path
;
use
std
:
:
sync
:
:
Arc
;
use
interrupt_support
:
:
SqlInterruptHandle
;
use
serde_json
:
:
Value
as
JsonValue
;
pub
struct
WebExtStorageStore
{
pub
(
crate
)
db
:
Arc
<
ThreadSafeStorageDb
>
}
impl
WebExtStorageStore
{
pub
fn
new
(
db_path
:
impl
AsRef
<
Path
>
)
-
>
Result
<
Self
>
{
let
db
=
StorageDb
:
:
new
(
db_path
)
?
;
Ok
(
Self
{
db
:
Arc
:
:
new
(
ThreadSafeStorageDb
:
:
new
(
db
)
)
}
)
}
#
[
cfg
(
test
)
]
pub
fn
new_memory
(
db_path
:
&
str
)
-
>
Result
<
Self
>
{
let
db
=
StorageDb
:
:
new_memory
(
db_path
)
?
;
Ok
(
Self
{
db
:
Arc
:
:
new
(
ThreadSafeStorageDb
:
:
new
(
db
)
)
}
)
}
pub
fn
interrupt_handle
(
&
self
)
-
>
Arc
<
SqlInterruptHandle
>
{
self
.
db
.
interrupt_handle
(
)
}
pub
fn
set
(
&
self
ext_id
:
&
str
val
:
JsonValue
)
-
>
Result
<
StorageChanges
>
{
let
db
=
&
self
.
db
.
lock
(
)
;
let
conn
=
db
.
get_connection
(
)
?
;
let
tx
=
conn
.
unchecked_transaction
(
)
?
;
let
result
=
api
:
:
set
(
&
tx
ext_id
val
)
?
;
tx
.
commit
(
)
?
;
Ok
(
result
)
}
pub
fn
usage
(
&
self
)
-
>
Result
<
Vec
<
crate
:
:
UsageInfo
>
>
{
let
db
=
&
self
.
db
.
lock
(
)
;
let
conn
=
db
.
get_connection
(
)
?
;
api
:
:
usage
(
conn
)
}
pub
fn
get
(
&
self
ext_id
:
&
str
keys
:
JsonValue
)
-
>
Result
<
JsonValue
>
{
let
db
=
&
self
.
db
.
lock
(
)
;
let
conn
=
db
.
get_connection
(
)
?
;
api
:
:
get
(
conn
ext_id
keys
)
}
pub
fn
get_keys
(
&
self
ext_id
:
&
str
)
-
>
Result
<
JsonValue
>
{
let
db
=
&
self
.
db
.
lock
(
)
;
let
conn
=
db
.
get_connection
(
)
?
;
api
:
:
get_keys
(
conn
ext_id
)
}
pub
fn
remove
(
&
self
ext_id
:
&
str
keys
:
JsonValue
)
-
>
Result
<
StorageChanges
>
{
let
db
=
&
self
.
db
.
lock
(
)
;
let
conn
=
db
.
get_connection
(
)
?
;
let
tx
=
conn
.
unchecked_transaction
(
)
?
;
let
result
=
api
:
:
remove
(
&
tx
ext_id
keys
)
?
;
tx
.
commit
(
)
?
;
Ok
(
result
)
}
pub
fn
clear
(
&
self
ext_id
:
&
str
)
-
>
Result
<
StorageChanges
>
{
let
db
=
&
self
.
db
.
lock
(
)
;
let
conn
=
db
.
get_connection
(
)
?
;
let
tx
=
conn
.
unchecked_transaction
(
)
?
;
let
result
=
api
:
:
clear
(
&
tx
ext_id
)
?
;
tx
.
commit
(
)
?
;
Ok
(
result
)
}
pub
fn
get_bytes_in_use
(
&
self
ext_id
:
&
str
keys
:
JsonValue
)
-
>
Result
<
u64
>
{
let
db
=
&
self
.
db
.
lock
(
)
;
let
conn
=
db
.
get_connection
(
)
?
;
Ok
(
api
:
:
get_bytes_in_use
(
conn
ext_id
keys
)
?
as
u64
)
}
pub
fn
close
(
&
self
)
-
>
Result
<
(
)
>
{
let
mut
db
=
self
.
db
.
lock
(
)
;
db
.
close
(
)
}
pub
fn
get_synced_changes
(
&
self
)
-
>
Result
<
Vec
<
sync
:
:
SyncedExtensionChange
>
>
{
let
db
=
self
.
db
.
lock
(
)
;
sync
:
:
get_synced_changes
(
&
db
)
}
pub
fn
migrate
(
&
self
filename
:
impl
AsRef
<
Path
>
)
-
>
Result
<
(
)
>
{
let
db
=
&
self
.
db
.
lock
(
)
;
let
conn
=
db
.
get_connection
(
)
?
;
let
tx
=
conn
.
unchecked_transaction
(
)
?
;
let
result
=
migrate
(
&
tx
filename
.
as_ref
(
)
)
?
;
tx
.
commit
(
)
?
;
if
let
Err
(
e
)
=
result
.
store
(
conn
)
{
debug_assert
!
(
false
"
Migration
error
:
{
:
?
}
"
e
)
;
warn
!
(
"
Failed
to
record
migration
telmetry
:
{
}
"
e
)
;
}
Ok
(
(
)
)
}
pub
fn
take_migration_info
(
&
self
)
-
>
Result
<
Option
<
MigrationInfo
>
>
{
let
db
=
&
self
.
db
.
lock
(
)
;
let
conn
=
db
.
get_connection
(
)
?
;
let
tx
=
conn
.
unchecked_transaction
(
)
?
;
let
result
=
MigrationInfo
:
:
take
(
&
tx
)
?
;
tx
.
commit
(
)
?
;
Ok
(
result
)
}
}
#
[
cfg
(
test
)
]
pub
mod
test
{
use
super
:
:
*
;
#
[
test
]
fn
test_send
(
)
{
fn
ensure_send
<
T
:
Send
>
(
)
{
}
ensure_send
:
:
<
WebExtStorageStore
>
(
)
;
}
pub
fn
new_mem_store
(
)
-
>
WebExtStorageStore
{
WebExtStorageStore
{
db
:
Arc
:
:
new
(
ThreadSafeStorageDb
:
:
new
(
crate
:
:
db
:
:
test
:
:
new_mem_db
(
)
)
)
}
}
}
