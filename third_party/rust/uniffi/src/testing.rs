use
anyhow
:
:
{
bail
Context
Result
}
;
use
camino
:
:
{
Utf8Path
Utf8PathBuf
}
;
use
cargo_metadata
:
:
Message
;
use
lazy_static
:
:
lazy_static
;
use
std
:
:
{
collections
:
:
HashMap
process
:
:
{
Command
Stdio
}
sync
:
:
Mutex
}
;
lazy_static
!
{
static
ref
COMPILED_COMPONENTS
:
Mutex
<
HashMap
<
Utf8PathBuf
Utf8PathBuf
>
>
=
Mutex
:
:
new
(
HashMap
:
:
new
(
)
)
;
/
/
Since
uniffi
-
bindgen
does
the
actual
generating
/
compiling
of
bindings
and
script
files
/
/
we
ensure
that
only
one
call
happens
at
once
(
making
tests
pretty
much
serialized
sorry
:
/
)
.
static
ref
UNIFFI_BINDGEN
:
Mutex
<
i32
>
=
Mutex
:
:
new
(
0
)
;
}
pub
fn
run_foreign_language_testcase
(
pkg_dir
:
&
str
udl_files
:
&
[
&
str
]
test_file
:
&
str
)
-
>
Result
<
(
)
>
{
let
cdylib_file
=
ensure_compiled_cdylib
(
pkg_dir
)
?
;
let
out_dir
=
cdylib_file
.
parent
(
)
.
context
(
"
Generated
cdylib
has
no
parent
directory
"
)
?
.
as_str
(
)
;
let
_lock
=
UNIFFI_BINDGEN
.
lock
(
)
;
run_uniffi_bindgen_test
(
out_dir
udl_files
test_file
)
?
;
Ok
(
(
)
)
}
pub
fn
ensure_compiled_cdylib
(
pkg_dir
:
&
str
)
-
>
Result
<
Utf8PathBuf
>
{
let
pkg_dir
=
Utf8Path
:
:
new
(
pkg_dir
)
;
let
mut
compiled_components
=
COMPILED_COMPONENTS
.
lock
(
)
.
unwrap
(
)
;
if
let
Some
(
cdylib_file
)
=
compiled_components
.
get
(
pkg_dir
)
{
return
Ok
(
cdylib_file
.
to_owned
(
)
)
;
}
let
mut
cmd
=
Command
:
:
new
(
"
cargo
"
)
;
cmd
.
arg
(
"
build
"
)
.
arg
(
"
-
-
message
-
format
=
json
"
)
.
arg
(
"
-
-
lib
"
)
;
cmd
.
current_dir
(
pkg_dir
)
;
cmd
.
stdout
(
Stdio
:
:
piped
(
)
)
;
let
mut
child
=
cmd
.
spawn
(
)
?
;
let
output
=
std
:
:
io
:
:
BufReader
:
:
new
(
child
.
stdout
.
take
(
)
.
unwrap
(
)
)
;
let
cdylibs
=
Message
:
:
parse_stream
(
output
)
.
filter_map
(
|
message
|
match
message
{
Err
(
e
)
=
>
Some
(
Err
(
e
.
into
(
)
)
)
Ok
(
Message
:
:
CompilerArtifact
(
artifact
)
)
=
>
{
if
artifact
.
target
.
kind
.
iter
(
)
.
any
(
|
item
|
item
=
=
"
cdylib
"
)
{
Some
(
Ok
(
artifact
)
)
}
else
{
None
}
}
_
=
>
None
}
)
.
collect
:
:
<
Result
<
Vec
<
_
>
>
>
(
)
?
;
if
!
child
.
wait
(
)
?
.
success
(
)
{
bail
!
(
"
Failed
to
execute
cargo
build
"
)
;
}
let
cdylib
=
match
cdylibs
.
len
(
)
{
0
=
>
bail
!
(
"
Crate
did
not
produce
any
cdylibs
it
must
not
be
a
uniffi
component
"
)
1
=
>
&
cdylibs
[
0
]
_
=
>
{
match
cdylibs
.
iter
(
)
.
find
(
|
cdylib
|
cdylib
.
target
.
src_path
.
starts_with
(
pkg_dir
)
)
{
Some
(
cdylib
)
=
>
{
log
:
:
warn
!
(
"
Crate
produced
multiple
cdylibs
using
the
one
produced
by
{
}
"
pkg_dir
)
;
cdylib
}
None
=
>
{
bail
!
(
"
Crate
produced
multiple
cdylibs
none
of
which
is
produced
by
{
}
"
pkg_dir
)
;
}
}
}
}
;
let
cdylib_files
:
Vec
<
_
>
=
cdylib
.
filenames
.
iter
(
)
.
filter
(
|
nm
|
matches
!
(
nm
.
extension
(
)
Some
(
std
:
:
env
:
:
consts
:
:
DLL_EXTENSION
)
)
)
.
collect
(
)
;
if
cdylib_files
.
len
(
)
!
=
1
{
bail
!
(
"
Failed
to
build
exactly
one
cdylib
file
it
must
not
be
a
uniffi
component
"
)
;
}
let
cdylib_file
=
cdylib_files
[
0
]
;
compiled_components
.
insert
(
pkg_dir
.
to_owned
(
)
cdylib_file
.
to_owned
(
)
)
;
Ok
(
cdylib_file
.
to_owned
(
)
)
}
#
[
cfg
(
not
(
feature
=
"
builtin
-
bindgen
"
)
)
]
fn
run_uniffi_bindgen_test
(
out_dir
:
&
str
udl_files
:
&
[
&
str
]
test_file
:
&
str
)
-
>
Result
<
(
)
>
{
let
udl_files
=
udl_files
.
join
(
"
\
n
"
)
;
let
status
=
Command
:
:
new
(
"
uniffi
-
bindgen
"
)
.
args
(
&
[
"
test
"
out_dir
&
udl_files
test_file
]
)
.
status
(
)
?
;
if
!
status
.
success
(
)
{
bail
!
(
"
Error
while
running
tests
:
{
}
"
status
)
;
}
Ok
(
(
)
)
}
#
[
cfg
(
feature
=
"
builtin
-
bindgen
"
)
]
fn
run_uniffi_bindgen_test
(
out_dir
:
&
str
udl_files
:
&
[
&
str
]
test_file
:
&
str
)
-
>
Result
<
(
)
>
{
uniffi_bindgen
:
:
run_tests
(
out_dir
udl_files
&
[
test_file
]
None
)
}
