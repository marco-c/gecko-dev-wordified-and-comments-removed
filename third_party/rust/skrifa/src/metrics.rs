use
read_fonts
:
:
{
tables
:
:
{
glyf
:
:
Glyf
gvar
:
:
Gvar
hmtx
:
:
LongMetric
hvar
:
:
Hvar
loca
:
:
Loca
os2
:
:
SelectionFlags
}
types
:
:
{
BigEndian
Fixed
GlyphId
}
FontRef
TableProvider
}
;
use
crate
:
:
{
outline
:
:
{
pen
:
:
ControlBoundsPen
DrawSettings
}
MetadataProvider
}
;
use
super
:
:
instance
:
:
{
LocationRef
NormalizedCoord
Size
}
;
pub
type
BoundingBox
=
read_fonts
:
:
types
:
:
BoundingBox
<
f32
>
;
#
[
derive
(
Copy
Clone
PartialEq
Default
Debug
)
]
pub
struct
Decoration
{
pub
offset
:
f32
pub
thickness
:
f32
}
#
[
derive
(
Copy
Clone
PartialEq
Default
Debug
)
]
pub
struct
Metrics
{
pub
units_per_em
:
u16
pub
glyph_count
:
u16
pub
is_monospace
:
bool
pub
italic_angle
:
f32
pub
ascent
:
f32
pub
descent
:
f32
pub
leading
:
f32
pub
cap_height
:
Option
<
f32
>
pub
x_height
:
Option
<
f32
>
pub
average_width
:
Option
<
f32
>
pub
max_width
:
Option
<
f32
>
pub
underline
:
Option
<
Decoration
>
pub
strikeout
:
Option
<
Decoration
>
pub
bounds
:
Option
<
BoundingBox
>
}
impl
Metrics
{
pub
fn
new
<
'
a
>
(
font
:
&
FontRef
<
'
a
>
size
:
Size
location
:
impl
Into
<
LocationRef
<
'
a
>
>
)
-
>
Self
{
let
head
=
font
.
head
(
)
;
let
mut
metrics
=
Metrics
{
units_per_em
:
head
.
map
(
|
head
|
head
.
units_per_em
(
)
)
.
unwrap_or_default
(
)
.
.
Default
:
:
default
(
)
}
;
let
coords
=
location
.
into
(
)
.
effective_coords
(
)
;
let
scale
=
size
.
linear_scale
(
metrics
.
units_per_em
)
;
if
let
Ok
(
head
)
=
font
.
head
(
)
{
metrics
.
bounds
=
Some
(
BoundingBox
{
x_min
:
head
.
x_min
(
)
as
f32
*
scale
y_min
:
head
.
y_min
(
)
as
f32
*
scale
x_max
:
head
.
x_max
(
)
as
f32
*
scale
y_max
:
head
.
y_max
(
)
as
f32
*
scale
}
)
;
}
if
let
Ok
(
maxp
)
=
font
.
maxp
(
)
{
metrics
.
glyph_count
=
maxp
.
num_glyphs
(
)
;
}
if
let
Ok
(
post
)
=
font
.
post
(
)
{
metrics
.
is_monospace
=
post
.
is_fixed_pitch
(
)
!
=
0
;
metrics
.
italic_angle
=
post
.
italic_angle
(
)
.
to_f64
(
)
as
f32
;
metrics
.
underline
=
Some
(
Decoration
{
offset
:
post
.
underline_position
(
)
.
to_i16
(
)
as
f32
*
scale
thickness
:
post
.
underline_thickness
(
)
.
to_i16
(
)
as
f32
*
scale
}
)
;
}
let
hhea
=
font
.
hhea
(
)
;
if
let
Ok
(
hhea
)
=
&
hhea
{
metrics
.
max_width
=
Some
(
hhea
.
advance_width_max
(
)
.
to_u16
(
)
as
f32
*
scale
)
;
}
let
os2
=
font
.
os2
(
)
.
ok
(
)
;
let
mut
used_typo_metrics
=
false
;
if
let
Some
(
os2
)
=
&
os2
{
if
os2
.
fs_selection
(
)
.
contains
(
SelectionFlags
:
:
USE_TYPO_METRICS
)
{
metrics
.
ascent
=
os2
.
s_typo_ascender
(
)
as
f32
*
scale
;
metrics
.
descent
=
os2
.
s_typo_descender
(
)
as
f32
*
scale
;
metrics
.
leading
=
os2
.
s_typo_line_gap
(
)
as
f32
*
scale
;
used_typo_metrics
=
true
;
}
metrics
.
average_width
=
Some
(
os2
.
x_avg_char_width
(
)
as
f32
*
scale
)
;
metrics
.
cap_height
=
os2
.
s_cap_height
(
)
.
map
(
|
v
|
v
as
f32
*
scale
)
;
metrics
.
x_height
=
os2
.
sx_height
(
)
.
map
(
|
v
|
v
as
f32
*
scale
)
;
metrics
.
strikeout
=
Some
(
Decoration
{
offset
:
os2
.
y_strikeout_position
(
)
as
f32
*
scale
thickness
:
os2
.
y_strikeout_size
(
)
as
f32
*
scale
}
)
;
}
if
!
used_typo_metrics
{
if
let
Ok
(
hhea
)
=
font
.
hhea
(
)
{
metrics
.
ascent
=
hhea
.
ascender
(
)
.
to_i16
(
)
as
f32
*
scale
;
metrics
.
descent
=
hhea
.
descender
(
)
.
to_i16
(
)
as
f32
*
scale
;
metrics
.
leading
=
hhea
.
line_gap
(
)
.
to_i16
(
)
as
f32
*
scale
;
}
if
metrics
.
ascent
=
=
0
.
0
&
&
metrics
.
descent
=
=
0
.
0
{
if
let
Some
(
os2
)
=
&
os2
{
if
os2
.
s_typo_ascender
(
)
!
=
0
|
|
os2
.
s_typo_descender
(
)
!
=
0
{
metrics
.
ascent
=
os2
.
s_typo_ascender
(
)
as
f32
*
scale
;
metrics
.
descent
=
os2
.
s_typo_descender
(
)
as
f32
*
scale
;
metrics
.
leading
=
os2
.
s_typo_line_gap
(
)
as
f32
*
scale
;
}
else
{
metrics
.
ascent
=
os2
.
us_win_ascent
(
)
as
f32
*
scale
;
metrics
.
descent
=
-
(
os2
.
us_win_descent
(
)
as
f32
*
scale
)
;
}
}
}
}
if
let
(
Ok
(
mvar
)
true
)
=
(
font
.
mvar
(
)
!
coords
.
is_empty
(
)
)
{
use
read_fonts
:
:
tables
:
:
mvar
:
:
tags
:
:
*
;
let
metric_delta
=
|
tag
|
mvar
.
metric_delta
(
tag
coords
)
.
unwrap_or_default
(
)
.
to_f64
(
)
as
f32
*
scale
;
metrics
.
ascent
+
=
metric_delta
(
HASC
)
;
metrics
.
descent
+
=
metric_delta
(
HDSC
)
;
metrics
.
leading
+
=
metric_delta
(
HLGP
)
;
if
let
Some
(
cap_height
)
=
&
mut
metrics
.
cap_height
{
*
cap_height
+
=
metric_delta
(
CPHT
)
;
}
if
let
Some
(
x_height
)
=
&
mut
metrics
.
x_height
{
*
x_height
+
=
metric_delta
(
XHGT
)
;
}
if
let
Some
(
underline
)
=
&
mut
metrics
.
underline
{
underline
.
offset
+
=
metric_delta
(
UNDO
)
;
underline
.
thickness
+
=
metric_delta
(
UNDS
)
;
}
if
let
Some
(
strikeout
)
=
&
mut
metrics
.
strikeout
{
strikeout
.
offset
+
=
metric_delta
(
STRO
)
;
strikeout
.
thickness
+
=
metric_delta
(
STRS
)
;
}
}
metrics
}
}
#
[
derive
(
Clone
)
]
pub
struct
GlyphMetrics
<
'
a
>
{
font
:
FontRef
<
'
a
>
size
:
Size
glyph_count
:
u32
fixed_scale
:
FixedScaleFactor
h_metrics
:
&
'
a
[
LongMetric
]
default_advance_width
:
u16
lsbs
:
&
'
a
[
BigEndian
<
i16
>
]
hvar
:
Option
<
Hvar
<
'
a
>
>
gvar
:
Option
<
Gvar
<
'
a
>
>
loca_glyf
:
Option
<
(
Loca
<
'
a
>
Glyf
<
'
a
>
)
>
coords
:
&
'
a
[
NormalizedCoord
]
}
impl
<
'
a
>
GlyphMetrics
<
'
a
>
{
pub
fn
new
(
font
:
&
FontRef
<
'
a
>
size
:
Size
location
:
impl
Into
<
LocationRef
<
'
a
>
>
)
-
>
Self
{
let
glyph_count
=
font
.
maxp
(
)
.
map
(
|
maxp
|
maxp
.
num_glyphs
(
)
as
u32
)
.
unwrap_or_default
(
)
;
let
upem
=
font
.
head
(
)
.
map
(
|
head
|
head
.
units_per_em
(
)
)
.
unwrap_or_default
(
)
;
let
fixed_scale
=
FixedScaleFactor
(
size
.
fixed_linear_scale
(
upem
)
)
;
let
coords
=
location
.
into
(
)
.
effective_coords
(
)
;
let
(
h_metrics
default_advance_width
lsbs
)
=
font
.
hmtx
(
)
.
map
(
|
hmtx
|
{
let
h_metrics
=
hmtx
.
h_metrics
(
)
;
let
default_advance_width
=
h_metrics
.
last
(
)
.
map
(
|
m
|
m
.
advance
.
get
(
)
)
.
unwrap_or
(
0
)
;
let
lsbs
=
hmtx
.
left_side_bearings
(
)
;
(
h_metrics
default_advance_width
lsbs
)
}
)
.
unwrap_or_default
(
)
;
let
hvar
=
font
.
hvar
(
)
.
ok
(
)
;
let
gvar
=
font
.
gvar
(
)
.
ok
(
)
;
let
loca_glyf
=
if
let
(
Ok
(
loca
)
Ok
(
glyf
)
)
=
(
font
.
loca
(
None
)
font
.
glyf
(
)
)
{
Some
(
(
loca
glyf
)
)
}
else
{
None
}
;
Self
{
font
:
font
.
clone
(
)
size
glyph_count
fixed_scale
h_metrics
default_advance_width
lsbs
hvar
gvar
loca_glyf
coords
}
}
pub
fn
glyph_count
(
&
self
)
-
>
u32
{
self
.
glyph_count
}
pub
fn
advance_width
(
&
self
glyph_id
:
GlyphId
)
-
>
Option
<
f32
>
{
if
glyph_id
.
to_u32
(
)
>
=
self
.
glyph_count
{
return
None
;
}
let
mut
advance
=
self
.
h_metrics
.
get
(
glyph_id
.
to_u32
(
)
as
usize
)
.
map
(
|
metric
|
metric
.
advance
(
)
)
.
unwrap_or
(
self
.
default_advance_width
)
as
i32
;
if
let
Some
(
hvar
)
=
&
self
.
hvar
{
advance
+
=
hvar
.
advance_width_delta
(
glyph_id
self
.
coords
)
.
map
(
|
delta
|
delta
.
to_f64
(
)
as
i32
)
.
unwrap_or
(
0
)
;
}
else
if
self
.
gvar
.
is_some
(
)
{
advance
+
=
self
.
metric_deltas_from_gvar
(
glyph_id
)
.
unwrap_or_default
(
)
[
1
]
;
}
Some
(
self
.
fixed_scale
.
apply
(
advance
)
)
}
pub
fn
left_side_bearing
(
&
self
glyph_id
:
GlyphId
)
-
>
Option
<
f32
>
{
if
glyph_id
.
to_u32
(
)
>
=
self
.
glyph_count
{
return
None
;
}
let
gid_index
=
glyph_id
.
to_u32
(
)
as
usize
;
let
mut
lsb
=
self
.
h_metrics
.
get
(
gid_index
)
.
map
(
|
metric
|
metric
.
side_bearing
(
)
)
.
unwrap_or_else
(
|
|
{
self
.
lsbs
.
get
(
gid_index
.
saturating_sub
(
self
.
h_metrics
.
len
(
)
)
)
.
map
(
|
lsb
|
lsb
.
get
(
)
)
.
unwrap_or_default
(
)
}
)
as
i32
;
if
let
Some
(
hvar
)
=
&
self
.
hvar
{
lsb
+
=
hvar
.
lsb_delta
(
glyph_id
self
.
coords
)
.
map
(
|
delta
|
delta
.
to_f64
(
)
as
i32
)
.
unwrap_or
(
0
)
;
}
else
if
self
.
gvar
.
is_some
(
)
{
lsb
+
=
self
.
metric_deltas_from_gvar
(
glyph_id
)
.
unwrap_or_default
(
)
[
0
]
;
}
Some
(
self
.
fixed_scale
.
apply
(
lsb
)
)
}
pub
fn
bounds
(
&
self
glyph_id
:
GlyphId
)
-
>
Option
<
BoundingBox
>
{
if
self
.
gvar
.
is_some
(
)
|
|
self
.
font
.
cff
(
)
.
ok
(
)
.
is_some
(
)
|
|
self
.
font
.
cff2
(
)
.
ok
(
)
.
is_some
(
)
{
return
self
.
bounds_from_outline
(
glyph_id
)
;
}
let
(
loca
glyf
)
=
self
.
loca_glyf
.
as_ref
(
)
?
;
Some
(
match
loca
.
get_glyf
(
glyph_id
glyf
)
.
ok
(
)
?
{
Some
(
glyph
)
=
>
BoundingBox
{
x_min
:
self
.
fixed_scale
.
apply
(
glyph
.
x_min
(
)
as
i32
)
y_min
:
self
.
fixed_scale
.
apply
(
glyph
.
y_min
(
)
as
i32
)
x_max
:
self
.
fixed_scale
.
apply
(
glyph
.
x_max
(
)
as
i32
)
y_max
:
self
.
fixed_scale
.
apply
(
glyph
.
y_max
(
)
as
i32
)
}
None
=
>
BoundingBox
:
:
default
(
)
}
)
}
}
impl
GlyphMetrics
<
'
_
>
{
fn
metric_deltas_from_gvar
(
&
self
glyph_id
:
GlyphId
)
-
>
Option
<
[
i32
;
2
]
>
{
let
(
loca
glyf
)
=
self
.
loca_glyf
.
as_ref
(
)
?
;
let
mut
deltas
=
self
.
gvar
.
as_ref
(
)
?
.
phantom_point_deltas
(
glyf
loca
self
.
coords
glyph_id
)
.
ok
(
)
.
flatten
(
)
?
;
deltas
[
1
]
-
=
deltas
[
0
]
;
Some
(
[
deltas
[
0
]
deltas
[
1
]
]
.
map
(
|
delta
|
delta
.
x
.
to_i32
(
)
)
)
}
fn
bounds_from_outline
(
&
self
glyph_id
:
GlyphId
)
-
>
Option
<
BoundingBox
>
{
if
let
Some
(
outline
)
=
self
.
font
.
outline_glyphs
(
)
.
get
(
glyph_id
)
{
let
settings
=
DrawSettings
:
:
unhinted
(
self
.
size
self
.
coords
)
;
let
mut
pen
=
ControlBoundsPen
:
:
default
(
)
;
outline
.
draw
(
settings
&
mut
pen
)
.
ok
(
)
?
;
pen
.
bounding_box
(
)
}
else
{
None
}
}
}
#
[
derive
(
Copy
Clone
)
]
struct
FixedScaleFactor
(
Fixed
)
;
impl
FixedScaleFactor
{
#
[
inline
(
always
)
]
fn
apply
(
self
value
:
i32
)
-
>
f32
{
self
.
0
.
mul_div
(
Fixed
:
:
from_bits
(
value
)
Fixed
:
:
from_bits
(
64
)
)
.
to_f32
(
)
}
}
#
[
cfg
(
test
)
]
mod
tests
{
use
super
:
:
*
;
use
font_test_data
:
:
{
NOTO_SANS_JP_CFF
SIMPLE_GLYF
VAZIRMATN_VAR
}
;
use
read_fonts
:
:
FontRef
;
#
[
test
]
fn
metrics
(
)
{
let
font
=
FontRef
:
:
new
(
SIMPLE_GLYF
)
.
unwrap
(
)
;
let
metrics
=
font
.
metrics
(
Size
:
:
unscaled
(
)
LocationRef
:
:
default
(
)
)
;
let
expected
=
Metrics
{
units_per_em
:
1024
glyph_count
:
3
bounds
:
Some
(
BoundingBox
{
x_min
:
51
.
0
y_min
:
-
250
.
0
x_max
:
998
.
0
y_max
:
950
.
0
}
)
average_width
:
Some
(
1275
.
0
)
max_width
:
None
x_height
:
Some
(
512
.
0
)
cap_height
:
Some
(
717
.
0
)
is_monospace
:
false
italic_angle
:
0
.
0
ascent
:
950
.
0
descent
:
-
250
.
0
leading
:
0
.
0
underline
:
None
strikeout
:
Some
(
Decoration
{
offset
:
307
.
0
thickness
:
51
.
0
}
)
}
;
assert_eq
!
(
metrics
expected
)
;
}
#
[
test
]
fn
metrics_missing_os2
(
)
{
let
font
=
FontRef
:
:
new
(
VAZIRMATN_VAR
)
.
unwrap
(
)
;
let
metrics
=
font
.
metrics
(
Size
:
:
unscaled
(
)
LocationRef
:
:
default
(
)
)
;
let
expected
=
Metrics
{
units_per_em
:
2048
glyph_count
:
4
bounds
:
Some
(
BoundingBox
{
x_min
:
29
.
0
y_min
:
0
.
0
x_max
:
1310
.
0
y_max
:
1847
.
0
}
)
average_width
:
None
max_width
:
Some
(
1336
.
0
)
x_height
:
None
cap_height
:
None
is_monospace
:
false
italic_angle
:
0
.
0
ascent
:
2100
.
0
descent
:
-
1100
.
0
leading
:
0
.
0
underline
:
None
strikeout
:
None
}
;
assert_eq
!
(
metrics
expected
)
;
}
#
[
test
]
fn
glyph_metrics
(
)
{
let
font
=
FontRef
:
:
new
(
VAZIRMATN_VAR
)
.
unwrap
(
)
;
let
glyph_metrics
=
font
.
glyph_metrics
(
Size
:
:
unscaled
(
)
LocationRef
:
:
default
(
)
)
;
let
expected
=
&
[
(
908
.
0
100
.
0
)
(
1336
.
0
29
.
0
)
(
1336
.
0
29
.
0
)
(
633
.
0
57
.
0
)
]
;
let
result
=
(
0
.
.
4
)
.
map
(
|
i
|
{
let
gid
=
GlyphId
:
:
new
(
i
as
u32
)
;
let
advance_width
=
glyph_metrics
.
advance_width
(
gid
)
.
unwrap
(
)
;
let
lsb
=
glyph_metrics
.
left_side_bearing
(
gid
)
.
unwrap
(
)
;
(
advance_width
lsb
)
}
)
.
collect
:
:
<
Vec
<
_
>
>
(
)
;
assert_eq
!
(
expected
&
result
[
.
.
]
)
;
}
#
[
test
]
fn
glyph_metrics_unscaled_matches_upem_scale
(
)
{
let
font
=
FontRef
:
:
new
(
VAZIRMATN_VAR
)
.
unwrap
(
)
;
let
upem
=
font
.
head
(
)
.
unwrap
(
)
.
units_per_em
(
)
as
f32
;
let
unscaled_metrics
=
font
.
glyph_metrics
(
Size
:
:
unscaled
(
)
LocationRef
:
:
default
(
)
)
;
let
upem_metrics
=
font
.
glyph_metrics
(
Size
:
:
new
(
upem
)
LocationRef
:
:
default
(
)
)
;
for
i
in
0
.
.
unscaled_metrics
.
glyph_count
(
)
{
let
gid
=
GlyphId
:
:
new
(
i
)
;
assert_eq
!
(
unscaled_metrics
.
advance_width
(
gid
)
upem_metrics
.
advance_width
(
gid
)
)
;
assert_eq
!
(
unscaled_metrics
.
left_side_bearing
(
gid
)
upem_metrics
.
left_side_bearing
(
gid
)
)
;
}
}
#
[
test
]
fn
glyph_metrics_var
(
)
{
let
font
=
FontRef
:
:
new
(
VAZIRMATN_VAR
)
.
unwrap
(
)
;
let
coords
=
&
[
NormalizedCoord
:
:
from_f32
(
-
0
.
8
)
]
;
let
glyph_metrics
=
font
.
glyph_metrics
(
Size
:
:
unscaled
(
)
LocationRef
:
:
new
(
coords
)
)
;
let
expected
=
&
[
(
908
.
0
100
.
0
)
(
1246
.
0
29
.
0
)
(
1246
.
0
29
.
0
)
(
556
.
0
57
.
0
)
]
;
let
result
=
(
0
.
.
4
)
.
map
(
|
i
|
{
let
gid
=
GlyphId
:
:
new
(
i
as
u32
)
;
let
advance_width
=
glyph_metrics
.
advance_width
(
gid
)
.
unwrap
(
)
;
let
lsb
=
glyph_metrics
.
left_side_bearing
(
gid
)
.
unwrap
(
)
;
(
advance_width
lsb
)
}
)
.
collect
:
:
<
Vec
<
_
>
>
(
)
;
assert_eq
!
(
expected
&
result
[
.
.
]
)
;
let
coords
=
&
[
NormalizedCoord
:
:
from_f32
(
-
1
.
0
)
]
;
let
glyph_metrics
=
font
.
glyph_metrics
(
Size
:
:
unscaled
(
)
LocationRef
:
:
new
(
coords
)
)
;
let
bounds
=
glyph_metrics
.
bounds
(
GlyphId
:
:
new
(
1
)
)
.
unwrap
(
)
;
assert_eq
!
(
bounds
BoundingBox
{
x_min
:
33
.
0
y_min
:
0
.
0
x_max
:
1189
.
0
y_max
:
1456
.
0
}
)
;
}
#
[
test
]
fn
glyph_metrics_cff
(
)
{
let
font
=
FontRef
:
:
new
(
NOTO_SANS_JP_CFF
)
.
unwrap
(
)
;
let
glyph_metrics
=
font
.
glyph_metrics
(
Size
:
:
unscaled
(
)
LocationRef
:
:
default
(
)
)
;
let
bounds
=
glyph_metrics
.
bounds
(
GlyphId
:
:
new
(
34
)
)
.
unwrap
(
)
;
assert_eq
!
(
bounds
BoundingBox
{
x_min
:
4
.
0
y_min
:
0
.
0
x_max
:
604
.
0
y_max
:
733
.
0
}
)
;
}
#
[
test
]
fn
glyph_metrics_missing_hvar
(
)
{
let
font
=
FontRef
:
:
new
(
VAZIRMATN_VAR
)
.
unwrap
(
)
;
let
glyph_count
=
font
.
maxp
(
)
.
unwrap
(
)
.
num_glyphs
(
)
;
for
coord
in
[
-
1
.
0
-
0
.
8
0
.
0
0
.
75
1
.
0
]
{
let
coords
=
&
[
NormalizedCoord
:
:
from_f32
(
coord
)
]
;
let
location
=
LocationRef
:
:
new
(
coords
)
;
let
glyph_metrics
=
font
.
glyph_metrics
(
Size
:
:
unscaled
(
)
location
)
;
let
mut
glyph_metrics_no_hvar
=
glyph_metrics
.
clone
(
)
;
glyph_metrics_no_hvar
.
hvar
=
None
;
for
gid
in
0
.
.
glyph_count
{
let
gid
=
GlyphId
:
:
from
(
gid
)
;
assert_eq
!
(
glyph_metrics
.
advance_width
(
gid
)
glyph_metrics_no_hvar
.
advance_width
(
gid
)
)
;
assert_eq
!
(
glyph_metrics
.
left_side_bearing
(
gid
)
glyph_metrics_no_hvar
.
left_side_bearing
(
gid
)
)
;
}
}
}
#
[
test
]
fn
match_freetype_glyph_metric_scaling
(
)
{
let
font_unit_advances
=
[
639
561
524
258
]
;
#
[
allow
(
clippy
:
:
excessive_precision
)
]
let
scaled_advances
=
[
15
.
33595275878906250000
13
.
46395874023437500000
12
.
57595825195312500000
6
.
19198608398437500000
]
;
let
fixed_scale
=
FixedScaleFactor
(
Size
:
:
new
(
24
.
0
)
.
fixed_linear_scale
(
1000
)
)
;
for
(
font_unit_advance
expected_scaled_advance
)
in
font_unit_advances
.
iter
(
)
.
zip
(
scaled_advances
)
{
let
scaled_advance
=
fixed_scale
.
apply
(
*
font_unit_advance
)
;
assert_eq
!
(
scaled_advance
expected_scaled_advance
)
;
}
}
}
