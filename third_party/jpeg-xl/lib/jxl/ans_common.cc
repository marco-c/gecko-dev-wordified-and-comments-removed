#
include
"
lib
/
jxl
/
ans_common
.
h
"
#
include
<
numeric
>
#
include
"
lib
/
jxl
/
ans_params
.
h
"
#
include
"
lib
/
jxl
/
base
/
status
.
h
"
namespace
jxl
{
std
:
:
vector
<
int32_t
>
CreateFlatHistogram
(
int
length
int
total_count
)
{
JXL_ASSERT
(
length
>
0
)
;
JXL_ASSERT
(
length
<
=
total_count
)
;
const
int
count
=
total_count
/
length
;
std
:
:
vector
<
int32_t
>
result
(
length
count
)
;
const
int
rem_counts
=
total_count
%
length
;
for
(
int
i
=
0
;
i
<
rem_counts
;
+
+
i
)
{
+
+
result
[
i
]
;
}
return
result
;
}
void
InitAliasTable
(
std
:
:
vector
<
int32_t
>
distribution
uint32_t
range
size_t
log_alpha_size
AliasTable
:
:
Entry
*
JXL_RESTRICT
a
)
{
while
(
!
distribution
.
empty
(
)
&
&
distribution
.
back
(
)
=
=
0
)
{
distribution
.
pop_back
(
)
;
}
if
(
distribution
.
empty
(
)
)
{
distribution
.
emplace_back
(
range
)
;
}
const
size_t
table_size
=
1
<
<
log_alpha_size
;
#
if
JXL_ENABLE_ASSERT
int
sum
=
std
:
:
accumulate
(
distribution
.
begin
(
)
distribution
.
end
(
)
0
)
;
#
endif
JXL_ASSERT
(
static_cast
<
uint32_t
>
(
sum
)
=
=
range
)
;
JXL_ASSERT
(
(
range
&
(
range
-
1
)
)
=
=
0
)
;
JXL_ASSERT
(
distribution
.
size
(
)
<
=
table_size
)
;
JXL_ASSERT
(
table_size
<
=
range
)
;
const
uint32_t
entry_size
=
range
>
>
log_alpha_size
;
for
(
size_t
sym
=
0
;
sym
<
distribution
.
size
(
)
;
sym
+
+
)
{
if
(
distribution
[
sym
]
=
=
ANS_TAB_SIZE
)
{
for
(
size_t
i
=
0
;
i
<
table_size
;
i
+
+
)
{
a
[
i
]
.
right_value
=
sym
;
a
[
i
]
.
cutoff
=
0
;
a
[
i
]
.
offsets1
=
entry_size
*
i
;
a
[
i
]
.
freq0
=
0
;
a
[
i
]
.
freq1_xor_freq0
=
ANS_TAB_SIZE
;
}
return
;
}
}
std
:
:
vector
<
uint32_t
>
underfull_posn
;
std
:
:
vector
<
uint32_t
>
overfull_posn
;
std
:
:
vector
<
uint32_t
>
cutoffs
(
1
<
<
log_alpha_size
)
;
for
(
size_t
i
=
0
;
i
<
distribution
.
size
(
)
;
i
+
+
)
{
cutoffs
[
i
]
=
distribution
[
i
]
;
if
(
cutoffs
[
i
]
>
entry_size
)
{
overfull_posn
.
push_back
(
i
)
;
}
else
if
(
cutoffs
[
i
]
<
entry_size
)
{
underfull_posn
.
push_back
(
i
)
;
}
}
for
(
uint32_t
i
=
distribution
.
size
(
)
;
i
<
table_size
;
i
+
+
)
{
cutoffs
[
i
]
=
0
;
underfull_posn
.
push_back
(
i
)
;
}
while
(
!
overfull_posn
.
empty
(
)
)
{
uint32_t
overfull_i
=
overfull_posn
.
back
(
)
;
overfull_posn
.
pop_back
(
)
;
JXL_ASSERT
(
!
underfull_posn
.
empty
(
)
)
;
uint32_t
underfull_i
=
underfull_posn
.
back
(
)
;
underfull_posn
.
pop_back
(
)
;
uint32_t
underfull_by
=
entry_size
-
cutoffs
[
underfull_i
]
;
cutoffs
[
overfull_i
]
-
=
underfull_by
;
a
[
underfull_i
]
.
right_value
=
overfull_i
;
a
[
underfull_i
]
.
offsets1
=
cutoffs
[
overfull_i
]
;
if
(
cutoffs
[
overfull_i
]
<
entry_size
)
{
underfull_posn
.
push_back
(
overfull_i
)
;
}
else
if
(
cutoffs
[
overfull_i
]
>
entry_size
)
{
overfull_posn
.
push_back
(
overfull_i
)
;
}
}
for
(
uint32_t
i
=
0
;
i
<
table_size
;
i
+
+
)
{
if
(
cutoffs
[
i
]
=
=
entry_size
)
{
a
[
i
]
.
right_value
=
i
;
a
[
i
]
.
offsets1
=
0
;
a
[
i
]
.
cutoff
=
0
;
}
else
{
a
[
i
]
.
offsets1
-
=
cutoffs
[
i
]
;
a
[
i
]
.
cutoff
=
cutoffs
[
i
]
;
}
const
size_t
freq0
=
i
<
distribution
.
size
(
)
?
distribution
[
i
]
:
0
;
const
size_t
i1
=
a
[
i
]
.
right_value
;
const
size_t
freq1
=
i1
<
distribution
.
size
(
)
?
distribution
[
i1
]
:
0
;
a
[
i
]
.
freq0
=
static_cast
<
uint16_t
>
(
freq0
)
;
a
[
i
]
.
freq1_xor_freq0
=
static_cast
<
uint16_t
>
(
freq1
^
freq0
)
;
}
}
}
