#
ifndef
MOZILLA_SCRATCHBUFFER_H_
#
define
MOZILLA_SCRATCHBUFFER_H_
#
include
<
mozilla
/
PodOperations
.
h
>
#
include
<
algorithm
>
namespace
mozilla
{
static
inline
uint32_t
FramesToSamples
(
uint32_t
aChannels
uint32_t
aFrames
)
{
return
aFrames
*
aChannels
;
}
static
inline
uint32_t
SamplesToFrames
(
uint32_t
aChannels
uint32_t
aSamples
)
{
MOZ_ASSERT
(
!
(
aSamples
%
aChannels
)
"
Frame
alignment
is
wrong
.
"
)
;
return
aSamples
/
aChannels
;
}
template
<
typename
T
>
class
AudioCallbackBufferWrapper
{
public
:
explicit
AudioCallbackBufferWrapper
(
uint32_t
aChannels
)
:
mBuffer
(
nullptr
)
mSamples
(
0
)
mSampleWriteOffset
(
1
)
mChannels
(
aChannels
)
{
MOZ_ASSERT
(
aChannels
)
;
}
void
SetBuffer
(
T
*
aBuffer
uint32_t
aFrames
)
{
MOZ_ASSERT
(
!
mBuffer
&
&
!
mSamples
"
SetBuffer
called
twice
.
"
)
;
mBuffer
=
aBuffer
;
mSamples
=
FramesToSamples
(
mChannels
aFrames
)
;
mSampleWriteOffset
=
0
;
}
void
WriteFrames
(
T
*
aBuffer
uint32_t
aFrames
)
{
MOZ_ASSERT
(
aFrames
<
=
Available
(
)
"
Writing
more
that
we
can
in
the
audio
buffer
.
"
)
;
PodCopy
(
mBuffer
+
mSampleWriteOffset
aBuffer
FramesToSamples
(
mChannels
aFrames
)
)
;
mSampleWriteOffset
+
=
FramesToSamples
(
mChannels
aFrames
)
;
}
uint32_t
Available
(
)
{
return
SamplesToFrames
(
mChannels
mSamples
-
mSampleWriteOffset
)
;
}
void
BufferFilled
(
)
{
NS_WARNING_ASSERTION
(
Available
(
)
=
=
0
|
|
mSampleWriteOffset
=
=
0
"
Audio
Buffer
is
not
full
by
the
end
of
the
callback
.
"
)
;
if
(
Available
(
)
)
{
PodZero
(
mBuffer
+
mSampleWriteOffset
FramesToSamples
(
mChannels
Available
(
)
)
)
;
}
MOZ_ASSERT
(
mSamples
"
Buffer
not
set
.
"
)
;
mSamples
=
0
;
mSampleWriteOffset
=
0
;
mBuffer
=
nullptr
;
}
private
:
T
*
mBuffer
;
uint32_t
mSamples
;
uint32_t
mSampleWriteOffset
;
uint32_t
const
mChannels
;
}
;
template
<
typename
T
uint32_t
BLOCK_SIZE
>
class
SpillBuffer
{
public
:
explicit
SpillBuffer
(
uint32_t
aChannels
)
:
mPosition
(
0
)
mChannels
(
aChannels
)
{
MOZ_ASSERT
(
aChannels
)
;
mBuffer
=
MakeUnique
<
T
[
]
>
(
BLOCK_SIZE
*
mChannels
)
;
PodZero
(
mBuffer
.
get
(
)
BLOCK_SIZE
*
mChannels
)
;
}
uint32_t
Empty
(
AudioCallbackBufferWrapper
<
T
>
&
aBuffer
)
{
uint32_t
framesToWrite
=
std
:
:
min
(
aBuffer
.
Available
(
)
SamplesToFrames
(
mChannels
mPosition
)
)
;
aBuffer
.
WriteFrames
(
mBuffer
.
get
(
)
framesToWrite
)
;
mPosition
-
=
FramesToSamples
(
mChannels
framesToWrite
)
;
if
(
mPosition
>
0
)
{
MOZ_ASSERT
(
FramesToSamples
(
mChannels
framesToWrite
)
+
mPosition
<
=
BLOCK_SIZE
*
mChannels
)
;
PodMove
(
mBuffer
.
get
(
)
mBuffer
.
get
(
)
+
FramesToSamples
(
mChannels
framesToWrite
)
mPosition
)
;
}
return
framesToWrite
;
}
uint32_t
Fill
(
T
*
aInput
uint32_t
aFrames
)
{
uint32_t
framesToWrite
=
std
:
:
min
(
aFrames
BLOCK_SIZE
-
SamplesToFrames
(
mChannels
mPosition
)
)
;
MOZ_ASSERT
(
FramesToSamples
(
mChannels
framesToWrite
)
+
mPosition
<
=
BLOCK_SIZE
*
mChannels
)
;
PodCopy
(
mBuffer
.
get
(
)
+
mPosition
aInput
FramesToSamples
(
mChannels
framesToWrite
)
)
;
mPosition
+
=
FramesToSamples
(
mChannels
framesToWrite
)
;
return
framesToWrite
;
}
private
:
UniquePtr
<
T
[
]
>
mBuffer
;
uint32_t
mPosition
;
uint32_t
const
mChannels
;
}
;
}
#
endif
