"
use
strict
"
;
const
{
HttpServer
}
=
ChromeUtils
.
importESModule
(
"
resource
:
/
/
testing
-
common
/
httpd
.
sys
.
mjs
"
)
;
let
httpserver
=
null
;
function
make_channel
(
url
)
{
return
NetUtil
.
newChannel
(
{
uri
:
url
loadUsingSystemPrincipal
:
true
}
)
;
}
const
responseBody
=
"
response
body
"
;
function
contentHandler
(
metadata
response
)
{
response
.
setHeader
(
"
Content
-
Type
"
"
text
/
plain
"
)
;
response
.
bodyOutputStream
.
write
(
responseBody
responseBody
.
length
)
;
}
function
setup_test_server
(
)
{
httpserver
=
new
HttpServer
(
)
;
httpserver
.
registerPathHandler
(
"
/
content
"
contentHandler
)
;
httpserver
.
start
(
-
1
)
;
}
registerCleanupFunction
(
async
(
)
=
>
{
if
(
httpserver
)
{
await
new
Promise
(
resolve
=
>
httpserver
.
stop
(
resolve
)
)
;
}
}
)
;
function
get_url
(
path
)
{
return
http
:
/
/
localhost
:
{
httpserver
.
identity
.
primaryPort
}
{
path
}
;
}
add_task
(
async
function
test_303_with_max_age
(
)
{
setup_test_server
(
)
;
let
requestCount
=
0
;
httpserver
.
registerPathHandler
(
"
/
redirect1
"
(
metadata
response
)
=
>
{
requestCount
+
+
;
response
.
setStatusLine
(
metadata
.
httpVersion
303
"
See
Other
"
)
;
response
.
setHeader
(
"
Location
"
get_url
(
"
/
content
"
)
false
)
;
response
.
setHeader
(
"
Cache
-
Control
"
"
max
-
age
=
3600
"
false
)
;
}
)
;
let
chan
=
make_channel
(
get_url
(
"
/
redirect1
"
)
)
;
let
buffer
=
await
new
Promise
(
resolve
=
>
{
chan
.
asyncOpen
(
new
ChannelListener
(
(
request
buf
)
=
>
resolve
(
buf
)
null
)
)
;
}
)
;
Assert
.
equal
(
buffer
responseBody
)
;
Assert
.
equal
(
requestCount
1
"
First
request
should
hit
server
"
)
;
chan
=
make_channel
(
get_url
(
"
/
redirect1
"
)
)
;
buffer
=
await
new
Promise
(
resolve
=
>
{
chan
.
asyncOpen
(
new
ChannelListener
(
(
request
buf
)
=
>
resolve
(
buf
)
null
)
)
;
}
)
;
Assert
.
equal
(
buffer
responseBody
)
;
Assert
.
equal
(
requestCount
1
"
Second
request
should
use
cached
redirect
"
)
;
}
)
;
add_task
(
async
function
test_303_with_expires
(
)
{
let
requestCount
=
0
;
httpserver
.
registerPathHandler
(
"
/
redirect2
"
(
metadata
response
)
=
>
{
requestCount
+
+
;
response
.
setStatusLine
(
metadata
.
httpVersion
303
"
See
Other
"
)
;
response
.
setHeader
(
"
Location
"
get_url
(
"
/
content
"
)
false
)
;
let
expires
=
new
Date
(
)
;
expires
.
setHours
(
expires
.
getHours
(
)
+
1
)
;
response
.
setHeader
(
"
Expires
"
expires
.
toUTCString
(
)
false
)
;
}
)
;
let
chan
=
make_channel
(
get_url
(
"
/
redirect2
"
)
)
;
let
buffer
=
await
new
Promise
(
resolve
=
>
{
chan
.
asyncOpen
(
new
ChannelListener
(
(
request
buf
)
=
>
resolve
(
buf
)
null
)
)
;
}
)
;
Assert
.
equal
(
buffer
responseBody
)
;
Assert
.
equal
(
requestCount
1
"
First
request
should
hit
server
"
)
;
chan
=
make_channel
(
get_url
(
"
/
redirect2
"
)
)
;
buffer
=
await
new
Promise
(
resolve
=
>
{
chan
.
asyncOpen
(
new
ChannelListener
(
(
request
buf
)
=
>
resolve
(
buf
)
null
)
)
;
}
)
;
Assert
.
equal
(
buffer
responseBody
)
;
Assert
.
equal
(
requestCount
1
"
Second
request
should
use
cached
redirect
"
)
;
}
)
;
add_task
(
async
function
test_303_without_cache_headers
(
)
{
let
requestCount
=
0
;
httpserver
.
registerPathHandler
(
"
/
redirect3
"
(
metadata
response
)
=
>
{
requestCount
+
+
;
response
.
setStatusLine
(
metadata
.
httpVersion
303
"
See
Other
"
)
;
response
.
setHeader
(
"
Location
"
get_url
(
"
/
content
"
)
false
)
;
}
)
;
let
chan
=
make_channel
(
get_url
(
"
/
redirect3
"
)
)
;
let
buffer
=
await
new
Promise
(
resolve
=
>
{
chan
.
asyncOpen
(
new
ChannelListener
(
(
request
buf
)
=
>
resolve
(
buf
)
null
)
)
;
}
)
;
Assert
.
equal
(
buffer
responseBody
)
;
Assert
.
equal
(
requestCount
1
"
First
request
should
hit
server
"
)
;
chan
=
make_channel
(
get_url
(
"
/
redirect3
"
)
)
;
buffer
=
await
new
Promise
(
resolve
=
>
{
chan
.
asyncOpen
(
new
ChannelListener
(
(
request
buf
)
=
>
resolve
(
buf
)
null
)
)
;
}
)
;
Assert
.
equal
(
buffer
responseBody
)
;
Assert
.
equal
(
requestCount
2
"
Second
request
should
hit
server
(
not
cached
)
"
)
;
}
)
;
add_task
(
async
function
test_303_with_no_store
(
)
{
let
requestCount
=
0
;
httpserver
.
registerPathHandler
(
"
/
redirect4
"
(
metadata
response
)
=
>
{
requestCount
+
+
;
response
.
setStatusLine
(
metadata
.
httpVersion
303
"
See
Other
"
)
;
response
.
setHeader
(
"
Location
"
get_url
(
"
/
content
"
)
false
)
;
response
.
setHeader
(
"
Cache
-
Control
"
"
no
-
store
"
false
)
;
}
)
;
let
chan
=
make_channel
(
get_url
(
"
/
redirect4
"
)
)
;
let
buffer
=
await
new
Promise
(
resolve
=
>
{
chan
.
asyncOpen
(
new
ChannelListener
(
(
request
buf
)
=
>
resolve
(
buf
)
null
)
)
;
}
)
;
Assert
.
equal
(
buffer
responseBody
)
;
Assert
.
equal
(
requestCount
1
"
First
request
should
hit
server
"
)
;
chan
=
make_channel
(
get_url
(
"
/
redirect4
"
)
)
;
buffer
=
await
new
Promise
(
resolve
=
>
{
chan
.
asyncOpen
(
new
ChannelListener
(
(
request
buf
)
=
>
resolve
(
buf
)
null
)
)
;
}
)
;
Assert
.
equal
(
buffer
responseBody
)
;
Assert
.
equal
(
requestCount
2
"
Second
request
should
hit
server
(
no
-
store
)
"
)
;
}
)
;
add_task
(
async
function
test_303_with_no_cache
(
)
{
let
requestCount
=
0
;
httpserver
.
registerPathHandler
(
"
/
redirect5
"
(
metadata
response
)
=
>
{
requestCount
+
+
;
response
.
setStatusLine
(
metadata
.
httpVersion
303
"
See
Other
"
)
;
response
.
setHeader
(
"
Location
"
get_url
(
"
/
content
"
)
false
)
;
response
.
setHeader
(
"
Cache
-
Control
"
"
max
-
age
=
3600
no
-
cache
"
false
)
;
}
)
;
let
chan
=
make_channel
(
get_url
(
"
/
redirect5
"
)
)
;
let
buffer
=
await
new
Promise
(
resolve
=
>
{
chan
.
asyncOpen
(
new
ChannelListener
(
(
request
buf
)
=
>
resolve
(
buf
)
null
)
)
;
}
)
;
Assert
.
equal
(
buffer
responseBody
)
;
Assert
.
equal
(
requestCount
1
"
First
request
should
hit
server
"
)
;
chan
=
make_channel
(
get_url
(
"
/
redirect5
"
)
)
;
buffer
=
await
new
Promise
(
resolve
=
>
{
chan
.
asyncOpen
(
new
ChannelListener
(
(
request
buf
)
=
>
resolve
(
buf
)
null
)
)
;
}
)
;
Assert
.
equal
(
buffer
responseBody
)
;
Assert
.
equal
(
requestCount
2
"
Second
request
should
revalidate
(
no
-
cache
)
"
)
;
}
)
;
